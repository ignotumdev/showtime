diff --git a/lib/CiaoService.d.ts b/lib/CiaoService.d.ts
index 278e31f0f396b0e098fef0bd448c5910c0a8b1b1..0f026be4af4d387dec97eaa4cac449233bd5f2b7 100644
--- a/lib/CiaoService.d.ts
+++ b/lib/CiaoService.d.ts
@@ -65,6 +65,11 @@ export interface ServiceOptions {
      * If undefined the service name will be used as default.
      */
     hostname?: string;
+    /**
+     * Reject advertisement when the requested name is already in use instead of
+     * silently selecting a different name.
+     */
+    fixedName?: boolean;
     /**
      * If defined, a txt record will be published with the given service.
      */
@@ -175,6 +180,7 @@ export type RecordsUpdateCallback = (error?: Error | null) => void;
 export declare interface CiaoService {
     on(event: "name-change", listener: (name: string) => void): this;
     on(event: "hostname-change", listener: (hostname: string) => void): this;
+    on(event: "republish-error", listener: (error: Error) => void): this;
     /**
      * @private
      */
diff --git a/lib/CiaoService.js b/lib/CiaoService.js
index a70f9b4906ccd9bd5d3d475eeef9d9ae076dacec..5319f56b380c062c97802a23cf730e674133b78b 100644
--- a/lib/CiaoService.js
+++ b/lib/CiaoService.js
@@ -151,6 +151,7 @@ class CiaoService extends events_1.EventEmitter {
         }
         this.hostname = domainFormatter.formatHostname(options.hostname || this.name, this.serviceDomain)
             .replace(/ /g, "-"); // replacing all spaces with dashes in the hostname
+        this.fixedName = options.fixedName === true;
         this.loweredHostname = (0, dns_equal_1.dnsLowerCase)(this.hostname);
         this.port = options.port;
         if (options.restrictedAddresses) {
@@ -473,11 +474,7 @@ class CiaoService extends events_1.EventEmitter {
             // thus, when sending the first request, the socket will be bound, and we don't need to wait here
             this.emit("republish" /* InternalServiceEvent.REPUBLISH */, error => {
                 if (error) {
-                    console.log("FATAL Error occurred trying to re-announce service " + this.fqdn + "! We can't recover from this!");
-                    console.log(error.stack);
-                    process.exit(1); // we have a service which should be announced, though we failed to re-announce.
-                    // if this should ever happen in reality, whe might want to introduce a more sophisticated recovery
-                    // for situations where it makes sense
+                    this.emit("republish-error", error); // Showtime owns recovery; never terminate the host process.
                 }
             });
         }
diff --git a/lib/Responder.js b/lib/Responder.js
index 78b01f6e889cc33766dab335223f34a8aa7323b7..700084f9c68ec42058d156ae3842121f747bff0f 100644
--- a/lib/Responder.js
+++ b/lib/Responder.js
@@ -240,6 +240,9 @@ class Responder {
             if (reason === Prober_1.Prober.CANCEL_REASON) {
                 callback();
             }
+            else if (service.fixedName) {
+                callback(reason);
+            }
             else { // other errors are only thrown when sockets error occur
                 console.log(`[${service.getFQDN()}] failed probing with reason: ${reason}. Trying again in 2 seconds!`);
                 return (0, promise_utils_1.PromiseTimeout)(2000).then(() => this.advertiseService(service, callback));
@@ -651,11 +654,7 @@ class Responder {
                 // noinspection JSIgnoredPromiseFromCall
                 this.republishService(service, error => {
                     if (error) {
-                        console.log(`FATAL Error occurred trying to resolve conflict for service ${service.getFQDN()}! We can't recover from this!`);
-                        console.log(error.stack);
-                        process.exit(1); // we have a service which should be announced, though we failed to reannounce.
-                        // if this should ever happen in reality, whe might want to introduce a more sophisticated recovery
-                        // for situations where it makes sense
+                        service.emit("republish-error", error); // Showtime owns recovery; never terminate the host process.
                     }
                 }, true);
             }
diff --git a/lib/responder/Prober.js b/lib/responder/Prober.js
index fdbdb4aa2397fd775e77ce7e902e8ef1085476a9..404cfa20af659e4f9d2f351ee7eb4397314eece9 100644
--- a/lib/responder/Prober.js
+++ b/lib/responder/Prober.js
@@ -194,6 +194,10 @@ class Prober {
     handleNameChange() {
         this.endProbing(false); // reset the prober
         this.service.serviceState = "unannounced" /* ServiceState.UNANNOUNCED */;
+        if (this.service.fixedName) {
+            this.promiseReject(new Error(`The fixed service name '${this.service.getHostname()}' is already in use`));
+            return;
+        }
         this.service.incrementName();
         this.service.serviceState = "probing" /* ServiceState.PROBING */;
         this.serviceEncounteredNameChange = true;
